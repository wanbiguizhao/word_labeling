{
    "version": "merge_fragments_gap5",
    "description": "合并窄碎片到相邻字符（width_ratio=0.5, max_gap=5, strategy=nearest）",
    "steps": [
        {
            "name": "merge_fragments",
            "enabled": true,
            "params": {
                "width_ratio": 0.5,
                "max_gap": 5,
                "strategy": "nearest"
            }
        }
    ]
}
